{
  "name": "reliadb-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:explain": "cd tools/explain && npm install && npm run build",
    "build:training": "cd tools/training && npm install && npm run build",
    "build": "npm run build:explain && npm run build:training && eleventy && pagefind --site _site --exclude-selectors \"nav,footer,#navbar,.site-footer\"",
    "dev": "eleventy --serve",
    "dev:explain": "cd tools/explain && npm run dev",
    "dev:training": "cd tools/training && npm run dev"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "markdown-it": "^14.1.0",
    "markdown-it-multimd-table": "^4.2.3",
    "pagefind": "^1.4.0"
  }
}
