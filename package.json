{
  "name": "reliadb-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "eleventy && pagefind --site _site --exclude-selectors \"nav,footer,#navbar,.site-footer\"",
    "dev": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "markdown-it": "^14.1.0",
    "markdown-it-multimd-table": "^4.2.3",
    "pagefind": "^1.4.0"
  }
}
